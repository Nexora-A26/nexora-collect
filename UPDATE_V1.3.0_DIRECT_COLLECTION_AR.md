# تحديث Nexora Collect v1.3.0 — القبض المباشر

هذا الإصدار يزيل صفحة **المبالغ المستحقة** من الواجهة ويجعل تسجيل القبض مباشراً.

## آلية العمل الجديدة

1. افتح صفحة **عمليات القبض**.
2. اضغط **قبض مباشر**.
3. اختر المندوب.
4. اختر العميل التابع للمندوب.
5. أدخل المبلغ المقبوض.
6. حدد نسبة العمولة؛ تُحمّل نسبة العميل الخاصة أو نسبة المندوب الافتراضية تلقائياً ويمكن تعديلها.
7. تظهر النتيجة فوراً:
   - المبلغ المقبوض.
   - قيمة عمولة المندوب.
   - صافي الإدارة.
8. احفظ العملية واطبع إيصال PDF عند الحاجة.

## تحديث قاعدة Supabase الموجودة

افتح Supabase ثم:

`SQL Editor → New query`

انسخ محتوى الملف التالي بالكامل وشغّله مرة واحدة:

`supabase/migrations/002_direct_collection_flow.sql`

لا تحذف الجداول القديمة ولا تشغّل ملف seed. يحافظ التحديث على البيانات السابقة، لكنه يخفي صفحة المبالغ المستحقة ويجعل العمليات الجديدة مستقلة عنها.

## مشروع Supabase جديد

شغّل فقط:

`supabase/migrations/001_nexora_collect.sql`

الملف محدث ويتضمن آلية القبض المباشر.

## رفع التحديث إلى GitHub

بعد استبدال ملفات المشروع داخل مجلد المستودع المحلي، افتح PowerShell ونفّذ:

```powershell
git add -A
git commit -m "Nexora Collect v1.3.0 direct collection"
git push origin main
```

سيبدأ Render النشر تلقائياً إذا كان Auto-Deploy مفعلاً. وإلا افتح Render واضغط **Manual Deploy → Deploy latest commit**.

## ترتيب التحديث الآمن

1. شغّل `002_direct_collection_flow.sql` في Supabase.
2. ارفع ملفات v1.3.0 إلى GitHub.
3. انتظر نجاح Render Deploy.
4. افتح الموقع وسجّل عملية قبض تجريبية صغيرة.
5. تحقق من العمولة وصافي الإدارة والأرصدة والتقارير.
