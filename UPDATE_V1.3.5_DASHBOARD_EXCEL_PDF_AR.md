# تحديث Nexora Collect v1.3.5

## 1. تحديث Supabase

افتح Supabase SQL Editor وشغّل الملف التالي مرة واحدة:

```text
supabase/migrations/005_dashboard_rebuild.sql
```

يعيد الملف بناء بيانات لوحة التحكم ويضيف القيم المطلوبة للبطاقات والرسوم مع احترام الصلاحيات.

## 2. التعديلات

- الداشبورد لا يبقى على شاشة التحميل عند حدوث خطأ.
- جميع القيم غير الموجودة تتحول إلى صفر بدلاً من إحداث خطأ.
- مخطط آخر 30 يوماً يعرض جميع الأيام حتى الأيام التي لا تحتوي عمليات.
- إضافة أفضل العملاء وإجمالي المبالغ المسلّمة.
- زر تحديث يدوي مع وقت آخر تحديث.
- Excel يحتوي شعار Nexora مضمناً في أعلى الورقة، ثم عنوان التقرير والتاريخ والجدول والإجمالي.
- PDF لا يعرض عمود صافي الإدارة في أي نوع تقرير، لكنه يحتفظ بصف الإجمالي لبقية الأعمدة.

## 3. رفع GitHub وRender

بعد استبدال ملفات المشروع:

```powershell
git add -A
git commit -m "Nexora Collect v1.3.5 dashboard and export fixes"
git push origin main
```

ثم في Render استخدم **Clear build cache & deploy**.
