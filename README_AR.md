# Nexora Collect

## تحديث v1.3.5 — إصلاح الداشبورد وتحديث التصدير

- إعادة بناء تحميل لوحة التحكم ومعالجة حالات الخطأ والبيانات الفارغة بدون تعليق الصفحة.
- إضافة أفضل العملاء وإجمالي المبالغ المسلّمة وتحديث يدوي للداشبورد.
- إضافة شعار Nexora مضمّناً داخل أعلى ورقة Excel مع عنوان التقرير وتاريخ التصدير.
- إزالة عمود **صافي الإدارة** من ملفات PDF فقط، مع بقائه داخل النظام وملفات Excel.
- لقواعد Supabase الموجودة مسبقاً شغّل `supabase/migrations/005_dashboard_rebuild.sql` مرة واحدة.

## تحديث v1.3.4 — تصدير إجمالي التقارير
تمت إضافة صف الإجمالي / Total في نهاية جداول التقارير وملفات PDF وExcel.


## تحديث v1.3.2 — حذف عمليات القبض نهائياً

زر حذف عملية القبض يزيل السجل نهائياً من قاعدة البيانات ويعيد احتساب الأرصدة والتقارير. شغّل `supabase/migrations/004_permanent_delete_collection.sql` على قواعد Supabase الموجودة مسبقاً.

# Nexora Collect — نكسورا للتحصيل

نظام عربي متكامل لإدارة المندوبين والعملاء وعمليات القبض المباشر والعمولات والأرصدة والتقارير، ويعمل على Render وSupabase أو كتطبيق Electron مكتبي.


## النسخة السحابية — Supabase + Vercel

ابتداءً من الإصدار 1.1.0 يعمل المشروع نفسه كتطبيق ويب حقيقي:

- Supabase Auth لتسجيل الدخول والمستخدمين.
- PostgreSQL في Supabase لحفظ المندوبين والعملاء والتحصيلات والتقارير.
- Row Level Security لتطبيق الصلاحيات داخل قاعدة البيانات.
- Vercel Functions لإنشاء المستخدمين وتغيير كلمات المرور والنسخ الاحتياطي.
- Vercel لاستضافة واجهة React/Vite.

ابدأ من الملف: `DEPLOY_SUPABASE_VERCEL_AR.md`.

## التقنية

- React 18
- Vite
- Electron
- TypeScript
- SQLite محلية عبر SQL.js
- Excel عبر ExcelJS مع دعم تضمين الشعار والتنسيق الاحترافي
- PDF عربي عبر محرك الطباعة الخاص بـ Chromium داخل Electron

## لا توجد بيانات تجريبية

قاعدة البيانات تبدأ فارغة بالكامل. عند أول تشغيل تظهر صفحة إنشاء مدير النظام. بعد إنشاء المدير تصبح شاشة تسجيل الدخول هي الصفحة الأولى.

## المستخدمون والصلاحيات

الأدوار المتاحة:

- مدير النظام
- مستخدم
- مشاهد

يستطيع المدير تحديد صلاحيات كل مستخدم بصورة مستقلة لكل صفحة:

- عرض
- إضافة
- تعديل
- حذف
- تصدير

المشاهد لا يستطيع الإضافة أو التعديل أو الحذف حتى إذا تم تحديدها خطأً، لأن المنع مطبق أيضاً داخل طبقة Electron وقاعدة البيانات وليس في الواجهة فقط.

## الصفحات

- تسجيل الدخول وإعداد المدير لأول مرة
- لوحة التحكم
- المندوبون
- العملاء ونقل العميل بين المندوبين
- عمليات القبض المباشر مع حساب العمولة وصافي الإدارة فوراً
- حساب عمولة المندوب وصافي الإدارة
- تسليمات المندوبين
- أرصدة المندوبين وملخص تحصيلات العملاء
- تقارير PDF وExcel
- المستخدمون والصلاحيات
- سجل العمليات
- إعدادات المؤسسة والعملة
- النسخ الاحتياطي والاستعادة

## العملة

العملة الافتراضية هي الدينار العراقي:

- الاسم: الدينار العراقي
- الكود: IQD
- الرمز: د.ع
- المنازل العشرية: 0

يمكن للمدير تغيير اسم العملة والكود والرمز ومكان الرمز والمنازل العشرية والفواصل. تغيير العملة يغيّر طريقة العرض فقط ولا يحوّل الأرقام المحفوظة تلقائياً.

## التشغيل على Windows

1. ثبّت Node.js إصدار 20 أو 22 LTS.
2. فك ضغط المشروع في مسار بسيط مثل `C:\NexoraCollect`.
3. شغّل `RUN_DEV_WINDOWS.bat` للتطوير.
4. عند أول تشغيل أنشئ حساب مدير النظام.

أوامر بديلة:

```bash
npm install
npm run dev
```

## الاختبار

شغّل:

```bash
npm run verify
```

وهو ينفذ:

1. فحص TypeScript.
2. بناء واجهة Vite.
3. اختبار تشغيل Electron وقاعدة البيانات مع خروج تلقائي.

## إنشاء Setup.exe ونسخة Portable

شغّل:

`BUILD_INSTALLER_WINDOWS.bat`

أو:

```bash
npm run dist:win
```

ستجد الملفات داخل مجلد `release`.

## مكان البيانات

تُحفظ قاعدة البيانات داخل مجلد بيانات التطبيق الخاص بالمستخدم تحت اسم:

`data/nexora-collect.sqlite`

استخدم صفحة الإعدادات لإنشاء النسخ الاحتياطية واستعادتها بدلاً من تعديل الملف يدوياً.

## قواعد مالية مطبقة

- لا يمكن قبض مبلغ أكبر من المتبقي.
- تدعم الدفعات الجزئية.
- العمولة = المبلغ المقبوض × النسبة ÷ 100.
- صافي الإدارة = المقبوض - العمولة.
- رصيد المندوب = صافي التحصيلات - التسليمات.
- تعديل أو إلغاء القبض يعيد حساب الفاتورة والرصيد تلقائياً.
- لا يُحذف مندوب أو عميل لديه سجل مالي؛ يتم إيقافه للحفاظ على التاريخ.
- نقل العميل لا يغيّر العمليات القديمة.
- جميع العمليات الحساسة مسجلة في سجل التدقيق.

## إصلاح بناء Windows في الإصدار 1.0.1

تم ضبط بناء Windows ليكون **Unsigned** افتراضياً، مع تعطيل `signAndEditExecutable`. يمنع ذلك تنزيل وتشغيل حزمة `winCodeSign` التي قد تفشل على Windows برسالة تفيد بعدم امتلاك صلاحية إنشاء symbolic link.

لبناء النسخة:

```bat
BUILD_INSTALLER_WINDOWS.bat
```

لا يلزم تشغيل الملف كمسؤول. سيُنشئ البناء ملفات المثبّت والنسخة المحمولة داخل مجلد `release`.

ملاحظة: بما أن التطبيق غير موقّع رقمياً، قد يعرض Windows SmartScreen تنبيهاً عند تشغيله على جهاز آخر. التوقيع الرقمي التجاري يمكن إضافته لاحقاً عند توفر شهادة Code Signing.

## رأس PDF الرسمي - الإصدار 1.0.7

تم اعتماد صورة شركة التكامل العربي كرأس رسمي موحّد لجميع ملفات PDF التي ينشئها البرنامج.

- تظهر الصورة في أعلى الصفحة الأولى.
- تتكرر تلقائياً أعلى كل صفحة في التقارير متعددة الصفحات.
- تُستخدم في تقارير النظام وإيصالات القبض وجميع عمليات التصدير التي تمر عبر محرك PDF المركزي.
- تم حجز مساحة أعلى الصفحة لمنع تداخل الرأس مع عنوان التقرير أو الجداول.
- ملف الصورة موجود داخل `electron/assets/altakamul-pdf-header.png` ويتم تضمينه داخل نسخة البرنامج المثبتة.


## إصلاح v1.0.7

تم إصلاح نوع أيقونات `lucide-react` في `src/components/Layout.tsx` باستخدام النوع الرسمي `LucideIcon` لمنع أخطاء TypeScript أثناء التحقق والبناء.

---

## النشر على Render

يتضمن الإصدار `v1.2.0` ملف `render.yaml` وخادم Express مخصصاً لتشغيل الواجهة وواجهات API من رابط Render واحد. اتبع الملف:

```text
DEPLOY_RENDER_AR.md
```

قاعدة البيانات والمصادقة تبقيان على Supabase.


## تحديث v1.3.0 — القبض المباشر

- أزيلت صفحة **المبالغ المستحقة** من القائمة والتنقل والصلاحيات.
- أصبحت عملية القبض مباشرة عبر اختيار المندوب والعميل والمبلغ ونسبة العمولة.
- يعرض النموذج فوراً قيمة العمولة وصافي المبلغ المستحق للإدارة قبل الحفظ.
- تم تحديث لوحة التحكم والأرصدة والتقارير وصفحات المندوبين والعملاء لتعمل على عمليات القبض المباشر.
- البيانات القديمة في جدول المبالغ المستحقة لا تُحذف، وتبقى محفوظة للتوافق والنسخ الاحتياطي.
- عند تحديث مشروع Supabase موجود مسبقاً، شغّل ملف `supabase/migrations/002_direct_collection_flow.sql`.


## إصلاح v1.3.1 — Trigger عمليات القبض

للمشاريع الموجودة، شغّل `supabase/migrations/003_fix_collection_code_trigger.sql` مرة واحدة في Supabase SQL Editor.
